import { expect } from 'chai';
import maxSliceSwap from '../src/maxSliceSwap';

describe ('Max Slice Swap', () => {

  it('Should be a function', () => {
    expect(maxSliceSwap).to.be.a('function');
  })

})