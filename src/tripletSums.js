// Given an array of integers and a number N, 
// find all the triplets that will add up to a sum greater than or equal to N.

var tripletSums = (arr, n) => {
  if (!Array.isArray(arr) || arr.length < 3 || typeof n !== 'number') {
    return null;
  }
}

export default tripletSums;