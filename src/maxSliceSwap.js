// Given an array, find the maximum sum from the set of subarrays after performing a single swap.

/*
** Example
** [-3, 5, -2, 3, 4, -2, 3] should be 13 because you swap 5 with -2
** [-3, 5, -2, 3, -4, 4, -2, 3] should be 13 because you swap 5 with -4
*/

var maxSliceSwap = arr => {

  

}

export default maxSliceSwap;